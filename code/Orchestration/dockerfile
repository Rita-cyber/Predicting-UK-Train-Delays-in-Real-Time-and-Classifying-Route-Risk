# Use official Apache Airflow base image
FROM apache/airflow:2.10.4

# Use ARG for Spark version (build-time variable)
#ARG SPARK_VERSION=3.5.1
#ARG HADOOP_VERSION=3

#USER root

# Install Java (required by Spark), curl, wget, unzip
#RUN apt-get update && \
    #apt-get install -y openjdk-17-jre-headless curl wget unzip && \
    #apt-get clean && rm -rf /var/lib/apt/lists/*

#ENV JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
#ENV PATH="${JAVA_HOME}/bin:${PATH}"

# Set Spark home and add Spark binaries to PATH
#ENV SPARK_HOME=/opt/spark
#ENV PATH="${SPARK_HOME}/bin:${SPARK_HOME}/sbin:${PATH}"

# Download and install Spark + Hadoop
#RUN curl -fsSL "https://archive.apache.org/dist/spark/spark-${SPARK_VERSION}/spark-${SPARK_VERSION}-bin-hadoop${HADOOP_VERSION}.tgz" \
    #| tar -xz -C /opt && \
    #mv /opt/spark-${SPARK_VERSION}-bin-hadoop${HADOOP_VERSION} ${SPARK_HOME}

# Download Hadoop AWS and AWS SDK bundles for S3 support in Spark
#RUN mkdir -p ${SPARK_HOME}/jars && \
    #wget -q https://repo1.maven.org/maven2/org/apache/hadoop/hadoop-aws/3.3.4/hadoop-aws-3.3.4.jar -P ${SPARK_HOME}/jars/ && \
    #wget -q https://repo1.maven.org/maven2/com/amazonaws/aws-java-sdk-bundle/1.12.367/aws-java-sdk-bundle-1.12.367.jar -P #${SPARK_HOME}/jars/

# Switch back to airflow user for security
USER airflow

# Copy requirements.txt to container
COPY requirements.txt /requirements.txt

# Upgrade pip and install python dependencies including pyspark pinned to SPARK_VERSION
RUN pip install --upgrade pip && \
    pip install --no-cache-dir -r /requirements.txt
    #pip install --no-cache-dir pyspark==${SPARK_VERSION}

# Expose Airflow default port
#EXPOSE 8080

# Default command
#CMD ["sh", "-c", "airflow webserver"]
